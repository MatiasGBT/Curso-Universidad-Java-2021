package test;

public class Explicacion {
	/*
	 Una interface es similar a una clase abstracta en el sentido de que los metodos de una
	 interface tampoco tienen una implementación.
	 Todos los metodos que definamos dentro de una interface van a ser abstractos,
	 si agregamos atributos a una interface, serán automaticamente constantes (public static final).
	 A diferencia de las clases abstractas, las relaciones entre clases no son por caracteristicas
	 si no por comportamientos
	*/
}
