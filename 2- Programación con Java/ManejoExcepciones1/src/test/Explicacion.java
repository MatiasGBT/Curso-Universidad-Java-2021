package test;

public class Explicacion {
	/*
	 En Java hay distintos tipos de excepciones. La clase padre de todas es la clase Throwable,
	 sin embargo, no es común usarla.
	 Por ende, se usamos 2 tipos de excepciones, las que heredan de la clase Exception
	 (las cuales son del tipo check, que indican que tenemos que utilizarlas con un bloque
	 de try y catch), y las que heredan de la clase RuntimeException (se presentan en tiempos
	 de ejecución y no es necesario el try/catch. El compilador no mostrara error.)
	*/
}
